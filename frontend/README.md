# AI热点监控工具前端

Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus + ECharts。

## 启动

```bash
npm install
npm run dev
```

默认代理后端：

- HTTP API: `http://localhost:8080/api`
- WebSocket: `ws://localhost:8080/ws`

默认登录账号来自后端环境变量，未配置时为 `admin / admin123`。
